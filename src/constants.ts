/**
 * The headlessId automatically given to the first AnswersHeadless instance created.
 */
export const DEFAULT_ID = 'main';

/**
 * A redux implementation detail. This is called when a store is first initialized, to set
 * the initial state.
 */
export const REDUX_INIT_ACTION = '@@INIT';